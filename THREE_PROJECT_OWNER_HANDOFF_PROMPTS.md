# 三项目 4080 所有权与交接提示词

> Owner v1 实施中的接入草案，暂勿作为生产切换命令转发。独立状态与接口以 `docs/OWNER_V1_DESIGN_2026-09-23.md` 和 `docs/OWNER_API_CONTRACT.md` 为准；三项目真实调用与交接验收完成前不得开启共享生产门禁

## 发送前说明

首版产品选择已经确定：H3 长视频占用 RTX 4080 时，Live 的 GPU 模型任务等待 H3 完成；Live 的录音持久化与确认回执继续工作。Manga 同样等待。不做抢占、优先级或自动迁往 2070 SUPER

这些提示词用于把已写入三个项目工作树的 Owner 路径继续审计、修复和验收；先读取现有改动及其他 Codex 正在进行的工作，不要重新实现一套同名机制。若同机有更高优先级的 GPU 工作，须等它结束再做真实任务测试。三个项目尚未统一采用新的所有权机制，当前共享 Broker 仍为观察模式；不得把“网页显示空闲”当成显卡空闲

## 三项目共同提示词

```text
目标：保护本机 RTX 4080，同一时刻最多让 H3、Live、Manga 中一个项目执行重型 GPU 工作。保留本项目原有队列、模型调用、取消、重试、结果和存储链路。gpu.aialra.online 用于观察和管理，不转发模型媒体，也不进入本机推理的公网必经路径

先读取本项目现有 Owner 工作树和当前运行配置，再列出所有可能加载或占用 GPU0 的入口：服务启动预热、后台队列恢复、自动重试、模型直连 HTTP、手动测试与清缓存。记录实际运行进程与队列；不要因为网页没有活动而认定 GPU 空闲。若其他任务正在使用 GPU，未经共同所有权约束或受控短时窗口，不提交不可安全抢占的 GPU 测试

设计“项目所有权”而非“每次模型调用一份业务任务镜像”：获取所有权必须早于模型加载，保持到本项目后台任务停止、相关模型卸载、后端回执及 GPU0 整卡用量核查结束。项目进程异常退出或后端结果不明时标为 UNKNOWN，并阻止下一项目自动获取。不得仅凭低显存或一个 HTTP 200 释放所有权

协调服务或监控网页故障时，已在运行的模型继续，不能仅因心跳超时主动取消它；新重任务只有在同一本机所有权原语仍能证明独占、并且后端与显存核查通过时才能运行，否则等待。业务网页、任务排队和 CPU 阶段继续可用。保留用户主动取消与按后端编号对账的通道。不要使用无条件 fail-open，避免旧任务还在运行时新任务绕过互斥

现有独立 Owner Core 已提供 FREE / OWNED / UNKNOWN 与 acquire / release / observe 契约；请按公开 API 合同接入和核对，不另建项目私有协调器。旧 Broker Session、Job、Permit 只作一次性迁移核查，不能改名后继续作为 Owner 底层。三项目完成真实验收前保留旧生产路径；迁移每条已验证入口时，同步删除该入口不再需要的旧强依赖。若以后采用进程间互斥量，必须验证持锁进程退出但模型子进程仍运行的 abandoned 情况

本机 Owner API 在 127.0.0.1:18767 使用每次请求随机挑战及双向 HMAC 签名，详细字段遵守 docs/OWNER_API_CONTRACT.md。项目令牌只在本机受限文件中读取，不能作为 Bearer 发给可能被错误进程抢占的回环端口；响应的挑战或签名不匹配时不得相信 ACQUIRED。公网 gpu.aialra.online 不在准入关键路径

交付：实际 GPU 入口表、最小改动方案、运行中故障语义、真实任务切换测试、可验证的卸载证明、误放行与误阻拦测试、与原模式的延迟对比。代码测试只覆盖逻辑；验收还需要 H3 视频、Live 真实录音模型任务和 Manga 原创单页结果。不要把服务启动、页面显示、模拟模型计数当作整体验收
```

## 发给 H3

```text
H3 当前生产仍经私有 Broker 获得许可后向 ComfyUI 提交。历史上发生过许可失联后桥接器取消仍在运行的 prompt；工作树已有对应保护、Owner bridge、Comfy gate、观察端及监护脚本 Owner 参数。请先只读复核这些实现、当前计划任务参数及并行改动，不重启运行中服务

先验证已写入的离线保护：仅因 Broker 心跳或读取失败而进入 UNCERTAIN 时，不能自动向仍在运行的 Comfy prompt 发取消请求；H3 必须保持该视频的所有权，阻止其他项目取得 4080，继续按原 prompt ID 跟踪 Comfy 历史与输出。用户主动取消、后端明确错误和确定性失败各保留原有语义。后端确已停、产物状态明确、模型卸载并核对后，才能释放 Owner；历史丢失则保留 UNKNOWN 人工核实。发现缺口时做最小修复并补有意义的回归

新 Owner 的窗口应覆盖一次 H3 重任务，而不是引用旧 batch_task Session ID；视频各阶段不应重新竞争跨项目所有权。旧 batch_task 仅供迁移时对账。不要通过虚报视频显存峰值让旧画像放行；需要按实际任务测量显存。测试需证明协调失联不杀视频、第二项目等待、正常结束后能交接，且视频结果可完整解码。其他正在运行的 GPU 工作优先，本轮不得抢占其时段
```

## 发给 Live

```text
Live 工作树已有 Core、Agent、Worker Owner 路径，但生产仍在旧 Worker 与共享 Ollama 上。先复核现有实现和测试，不重复搭建。Core 的录音落盘、ACK、业务队列和模型结果写回继续由 Live 自己负责。H3 持有 RTX 4080 时，新的 GPU 模型任务等待；录音和任务持久化继续，界面清楚显示识别／翻译延迟与积压，不暗示仍可实时完成。H3 释放后，Live Agent 按原业务队列恢复模型处理

核查 GPU Agent、Worker 和 Ollama 的加载、直接调用、启动探针、重试及驻留模型。Live 获取一个有限的项目 Owner 窗口后，才允许相关模型加载；窗口可覆盖一个已领取的 Core job 所需的多个模型阶段，不为每次模型 HTTP 调用重新竞争跨项目所有权，也不能因为 Core 不断出现新任务就永久占卡。释放前确认本轮 Worker 推理已结束、专属模型已卸载、Core 已持久写回或安全归还该 job，并核对 GPU0 整卡基线；未完成的 Core 任务仍在持久队列等待下一窗口。协调失联时，已提交的模型任务继续且保留 Owner；下一未提交阶段只有同进程已签名确认的原窗口和本机最终门禁仍可证明安全时才可继续，新的 Owner 获取等待，不直接绕过

对比旧模式真实录音闭环与所有权模式的 ACK、识别、翻译、积压清空、重复结果和延迟。不要在其他高优先级任务仍可能提交时停用服务或重建模型目录；独立 Ollama 是否必要，要以卸载隔离的实测决定，不因新方案名称改变就盲目删除
```

## 发给 Manga

```text
Manga 工作树已有 Owner 主流程、签名观察端和语义/FLUX Worker 门禁，生产仍在旧模式。先复核这些实现和所有旁路入口，不重复搭建。保留 Manga 的持久单页队列。语义缓存未命中与 FLUX 图像生成需要 4080 时，分别在对应模型工作开始前取得 Manga Owner；如果两段连续执行，可由同一窗口覆盖，若中间持久等待或转入其他业务步骤，须先安全释放再重新获取，不能无限期占卡。每段都须等模型请求停止、对应结果落盘、模型卸载与交接事实核实后释放；整页验收仍须检查语义、生成和最终页面产物。CPU palette 与纯缓存命中不应等待 GPU 所有权

检查导入、页面处理、修正、后台恢复、语义服务和 FLUX 直连入口都遵守同一 Owner。Cobra 当前不能证明真实卸载，正式所有权切换时继续禁用它，直到以独立进程退出或可靠的模型卸载证据验证。须完成原创单页的真实语义、FLUX、最终页面产物与画质检查，不能把健康接口的 200 当作验收

在 H3 和 Live 高优先级任务可能随时开始的时段，不启动不可安全抢占的 FLUX 推理。先做隔离测试：旧 Owner 活动时 Manga 等待且模型计数为零；正常释放后它能获得 Owner；异常退出且模型子进程仍运行时不得误放行；完全空闲后不得无故久等
```

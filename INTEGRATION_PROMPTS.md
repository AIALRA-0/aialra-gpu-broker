# H3、Live、Manga 的当前协同提示词

本文件按 [2026-09-22 实测复核](GPU_CALL_REVIEW_2026-09-22.md) 更新。当前决定是保留三个项目已有的模型调用路径和内部队列，先解决单卡切换、残留模型和任务恢复。`gpu.aialra.online` 目前是准入、状态与监控接口，**不是模型数据代理**；现阶段不要把业务推理、音视频或图片强制改经该域名，也不要在三个项目中擅自开启全局强制准入。

把下面的共用段和项目专用段交给相应项目维护者即可。后续若实测证实需要跨项目任务级互斥，再共同设计接入同一个 Broker 实例；接入时后端才使用项目令牌，浏览器始终不接触令牌。接口契约见 [API.md](API.md)。

## 共用段

```text
请先审计本项目的真实 GPU 调用链、排队/取消/恢复、模型预热与空闲驻留，不预设全部请求必须经过 GPU Broker。保留当前业务后端与模型之间的数据路径，以及已经可靠运行的项目内部队列。

记录每类任务的开始、结束、后端编号、显存峰值和空闲占用；任务状态不明时先查后端和产物，再允许切换或重试。模型仍在处理时禁止仅因 /health 超时而杀进程。切换到别的项目之前，确认本项目没有运行中或等待中的 GPU 任务，安全释放闲置模型，核对显存和进程。

评估共享 Broker 只作为监控/诊断及未来低频任务级互斥的可选层。不要把每个媒体分片或每个推理步骤变成公网同步申请。发现真实跨项目抢卡、等待或切换失败时，提交脱敏时间线和后端编号，再决定是否共同接入同一个 Broker。未经三项目联合验证，不开启全局强制准入。

交付调用入口清单、实际显存和任务切换记录、可重复的故障/恢复试验、残留任务处理方法及回退步骤。明确区分已实测事实、可能的冲突和待验证方案。
```

## H3 专用段

```text
H3 现有路径是网站 → H3 后端 → 专用 Broker 18766 → ComfyUI 8188。请保持任务编号持久化、ACTIVE 后提交、取消和恢复逻辑。检查图像、视频、放大等实际启用的 ComfyUI 入口是否走同一提交入口；对未验证路径只列为待测，不宣称已受控。

实测一张 512×512 图像任务完成后，ComfyUI 空闲缓存仍占显存。制定只在 ComfyUI 队列和专用 Broker 许可均为空时调用 /free 的操作流程，并记录释放前后占用。专用 Broker 与公共 18765 不是同一实例；它当前不能代表 Live、Manga 做跨项目互斥。若以后需要统一准入，先设计现有许可和公共实例之间的迁移及故障回退。
```

## Live 专用段

```text
Live 现有路径是网站/音频入口 → Live Core 租约队列 → GPU Agent → 模型 Worker/Ollama。保持音频先落盘、租约恢复、实时响应、模型预热和内部串行门控。验证静音识别、短句翻译、长时会话、重连和模型空闲占用。

若将来需要共享准入，粒度应是整段实时会话及其预热窗口；不要对每个音频片段向公网申请许可。必须测量新增等待、断线、取消、音频不丢失与译文不重复之后，才允许正式接入。
```

## Manga / PanelTone 专用段

```text
Manga 现有路径是工作台 8765 → 单任务持久队列 → FLUX 8781，并有其他本地模型服务。保持处理单元清单、排队和恢复机制；验证完整页面任务确实产出可读取的结果，而不只检查 /health。记录模型加载、推理、空闲 60 秒释放和显存变化。

监护进程发现 /health 超时时，先核对归属进程、监听端口和任务状态。进程仍在加载或推理时不得强杀；若进程长期存活却真正卡死，需要人工核对后端状态及产物，再按恢复流程处理。未来若接入公共 Broker，以整项任务或可安全停止的处理单元作为候选粒度，先测试超时、取消和重启恢复。
```

## 联合验收

在三类任务各自完成时记录基线，再测试 H3 → Live、Live → Manga、Manga → H3 的顺序切换；每次确认旧任务终态、模型释放、显存回落、新任务完成与结果只发布一次。只有观察到真实跨项目冲突并证实轻量切换约束不足时，才推进公共 Broker 的强制任务级许可。

# H3、Live、Manga 的 GPU 调用与 502 故障复核

## 1. 证据边界

- **当前事实**：`gpu.aialra.online` 的 `/ready` 与三个项目接口曾同时返回 502；公网代理到 VPS 的私有端口仍可连接，但反向隧道指向的本机 `127.0.0.1:18765` 没有监听，Windows 任务处于 `Ready`，服务日志记录进程已退出
- **历史故障**：日志还记录过磁盘写满造成 SQLite 写入失败；当前数据库完整性检查通过且数据盘有可用空间，不能把该历史记录直接认定为本次退出原因
- **用户提供的使用事实**：三类任务通常分别运行，几乎不主动混用；现有日志没有完整的跨项目任务时间线，无法据此计算实际并发频率
- **本轮实测**：公网恢复后，三个项目令牌均完成心跳、登记任务和终态更新；H3 网站入口完成一张真实生图；Live 本机模型完成静音识别与短句翻译；Manga 工作台的轻量整页任务完成并能读取产物，FLUX 模型推理成功返回图像但任务未通过现有画质校验

## 2. 真实调用链

| 项目 | 当前业务路径 | 已有资源控制 | 跨项目缺口 |
| --- | --- | --- | --- |
| H3 | 网站 → H3 后端 → 专用 Broker `127.0.0.1:18766` → ComfyUI `127.0.0.1:8188` | 后端持久保存任务与 ComfyUI 编号，正式许可为 `ACTIVE` 后提交，ComfyUI 提交入口另有口令检查 | 专用 Broker 不与 Live、Manga 共享许可；ComfyUI 模型缓存会在任务完成后继续占显存 |
| Live | 网站与音频入口 → Live Core 租约队列 → GPU Agent → 模型 Worker `127.0.0.1:8790`，另使用 Ollama | 音频先落盘，任务按租约恢复；Worker 序列化 GPU 调用，实时模型预热并驻留 | 没有调用共享 Broker；空闲驻留模型仍占显存，跨项目切换时可能压缩后续任务余量 |
| Manga | 工作台 `127.0.0.1:8765` → 单线程持久队列 → FLUX 服务 `127.0.0.1:8781` | 每页串行、模型内部推理锁、步骤间取消、默认空闲 60 秒卸载 | 没有调用共享 Broker；旧监护脚本会因健康接口暂时超时而终止仍在执行的进程 |

正常网站请求无需通过 GPU Broker，模型数据也没有统一代理接口；当前公开域名主要负责准入接口、状态与监控

## 3. 方案比较与选择

| 方案 | 适合当前项目的部分 | 当前代价或风险 | 判断 |
| --- | --- | --- | --- |
| 保留三个项目的原调用链 | H3 已有 ComfyUI 适配，Live 有低延迟租约与预热，Manga 有持久单任务队列；数据保持本机流动 | 单靠各自队列看不到其他项目的模型驻留和残留进程 | 保留作为模型调用路径 |
| 将所有模型请求经 `gpu.aialra.online` 转发 | 可以形成统一入口 | 现有 Broker 没有媒体传输、流式响应、取消传播与模型后端代理；新增公网往返和故障依赖，对 Live 的短片段尤其不合适 | 当前不实施 |
| 每个模型阶段均调用共享 Broker 申请许可 | 可统一展示等待、冲突和审计 | 三项目接入点众多，H3 已使用另一实例；Live 若按音频片段逐次申请会叠加现有租约并增加延迟 | 仅在实测出现跨项目争用后，按整项任务或实时会话接入同一实例 |
| 原调用链加轻量切换约束 | 只需在切换时确认旧任务停止、释放旧模型、检查显存，再启动新任务 | 依赖明确的切换入口及异常恢复规范，尚不能阻止同账号直接调用模型端口 | 当前优先方案 |

本轮公网项目接口的 5 次同机读取中位数为 16 ms，经过公开域名的 5 次读取中位数为 1,001 ms；这是当前负载下的准入接口样本，不代表所有网络时段。Live 一次静音识别为 212 ms，一次短句翻译为 1,086 ms；由此推断每个音频片段都经公网申请许可会明显增加响应时间。H3 真实生图结束后，确认队列和许可为空再释放缓存，受管卡占用从 14,764 MiB 降至 7,884 MiB；Manga 加载期间曾接近 15,971 MiB，说明残留模型即使没有并发用户任务，也会影响切换

**推荐定位**：保留各项目现有模型调用和内部队列，把 GPU Broker 用于监控、诊断和未来确有需要的低频任务级互斥。当前先执行单卡独占的切换规则，优先解决残留模型、进程退出恢复和不确定状态核对；不建设统一推理代理或复杂公平队列

分别落实时，H3 保留专用 Broker 与 ComfyUI 提交入口，增加任务结束后的安全清缓存检查；Live 保留可恢复租约与本机串行门控，把预热及常驻显存纳入切换核对；Manga 保留持久单任务队列，修复监护进程对长时间模型调用的误杀。三者当前都不需按媒体片段向公开域名申请许可，切换仍需人工核对真实后端状态，不能宣称已经自动完成跨项目互斥

## 4. 成熟方案取舍

- Windows 的 [Job Objects](https://learn.microsoft.com/en-us/windows/win32/procthread/job-objects) 可以让监督进程管理其子进程生命周期；若以后需要统一启动和停止三个引擎，它比把媒体经公网代理更接近本机问题
- Windows [互斥对象](https://learn.microsoft.com/en-us/windows/win32/sync/about-synchronization) 可作为跨进程排他保险，但异常退出后的占用必须核对模型进程和后端任务，不能仅凭锁释放认定显卡安全
- [Prefect 工作流](https://docs.prefect.io/latest/tutorial/flows) 更适合以后出现大量多阶段可恢复任务；[Ray Serve](https://docs.ray.io/en/latest/serve/architecture.html) 针对服务副本与扩缩；[NVIDIA MPS](https://docs.nvidia.com/deploy/mps/when-to-use-mps.html) 的目标是 CUDA 进程共享且不支持本机 Windows 用法，这些都没有解决本轮最主要的切换与进程恢复问题

## 5. 可执行的切换规则

1. 切换前确认当前项目没有正在运行、排队或结果不明的任务；结果不明时先查原后端编号及产物
2. 当前项目的后端任务确认停止后，释放闲置模型缓存；H3 只在 ComfyUI 队列及许可均为空时调用 `/free`，Manga 只在模型服务返回空闲时调用 `/release`
3. 检查受管 GPU 显存和后端进程，确认旧模型已退出或达到已测量的空闲基线，再启动下一项目任务
4. Live 音频采集和保存保持独立；若未来需要共享准入，保护整段实时会话与预热窗口，不能把每个音频片段放进公网许可轮询
5. 若用户实际开始频繁交替或同时提交三类任务，再把三个项目接到同一任务级准入入口，实测等待时间、取消、断线恢复和显存回收后才开启全局强制模式

## 6. 本轮修复与验证记录

- Broker 启动任务改为进程退出后持续重试，Windows 计划任务改为可在电池供电时继续运行；安装初次健康检查失败时保留已登记的监护任务
- SQLite 发生自动回滚时保留原始异常，轮询失败日志按 60 秒限速，避免同一磁盘故障不断放大日志量
- Manga 监护脚本在有归属的进程仍存活而 HTTP 健康暂时失效时保留该进程；现场原脚本曾在模型加载期间强制终止模型及工作台，修复后同类超时记录为保留进程
- 公网 `/ready` 恢复 200；三个项目无令牌接口恢复 401，带项目令牌的心跳、任务登记、终态更新均成功；本机 Broker 数据库完整性检查和备份通过
- H3 网站真实图像任务完成，生成文件可通过网站地址读取；Live 的静音识别和短句翻译调用成功
- Manga 首个合成页任务完成实际 FLUX 推理，但严格画质检查因局部未着色将其置为 `needs_attention`；同一极简图改用完整画风仍因大片区域回退到原图而未通过颜色保留检查；仓库自带的公开合成示例页也未通过颜色保留检查，这些模型 HTTP 请求均成功返回，并非 502，但不能作为 FLUX 任务完成验收
- 公开合成示例页的 FLUX 验收中，受管显卡曾占用 15,891 MiB，系统剩余物理内存降至约 0.4 GiB；为保护同机服务，通过 Manga 任务接口取消并归档了该诊断任务，没有强杀模型进程。随后以 `palette` 引擎提交同一示例页，整页任务状态为 `completed`、`qa_passed`，最终 PNG 经工作台页面接口返回 200，大小 155,053 字节；三个失败或取消的诊断任务已归档

当前仅凭这些样例不能证明长期可用，也不能证明三类任务任意组合同时运行无冲突；较复杂的统一调度应由连续使用记录和任务切换测试触发。Manga 的 FLUX 图像结果与画质校验不一致，以及高内存占用，是独立于本次 502 的待处理问题；在它通过真实页面端到端验收前，不应宣称该引擎稳定完成任务
